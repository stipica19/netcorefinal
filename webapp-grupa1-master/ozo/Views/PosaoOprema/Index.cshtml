@model IEnumerable<ozo.Models.PosaoOprema>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Oprema)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Posao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Oprema.Naziv)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Posao.Opis)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PosaoOpremaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PosaoOpremaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PosaoOpremaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
